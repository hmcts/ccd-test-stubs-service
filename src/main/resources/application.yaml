server:
  port: 5555

management:
  endpoints:
    web:
      base-path: /

spring:
  application:
    name: CCD Stub Callbak Service

callback:
  json:
    stubs:
      path: "${CALLBACK_JSON_STUBS_PATH}"
